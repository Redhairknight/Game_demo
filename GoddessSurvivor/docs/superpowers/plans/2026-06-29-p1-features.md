# P1 Features Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补全游戏的 P1 功能，使游戏拥有完整的三武器系统、角色选择界面、暂停/结束界面，让一局游戏能从开始到结束完整运行。

**Architecture:** 每个 Task 是独立可测试的单元。武器场景（Task 1）解决 SpinBlade/ChainLightning 无法被实例化的问题；角色选择界面（Task 2）替换硬编码的 "rin"，通过 GameManager.current_character_id 传递选择；暂停/结束界面（Task 3-4）挂在 CanvasLayer 上，通过 GameManager.state_changed 信号驱动显示/隐藏。

**Tech Stack:** Godot 4.6, GDScript, 无外部依赖

---

## 文件变更总览

### 新建
- `scenes/weapons/spin_blade.tscn` — SpinBlade 武器场景（纯代码生成，无外部资源）
- `scenes/weapons/chain_lightning.tscn` — ChainLightning 武器场景
- `scenes/ui/character_select.tscn` + `scripts/ui/character_select.gd` — 角色选择界面
- `scenes/ui/pause_menu.tscn` + `scripts/ui/pause_menu.gd` — 暂停界面
- `scenes/ui/game_over.tscn` + `scripts/ui/game_over.gd` — 游戏结束界面
- `scenes/main/boot.tscn` — 启动场景（先显示角色选择，再进入 main.tscn）

### 修改
- `scripts/systems/main_game.gd` — 从 GameManager.current_character_id 读取角色，移除硬编码 "rin"
- `scripts/ui/level_up_panel.gd` — 武器选项改用 preload 路径加载场景（已有 .new() 方式，保留不变）
- `scenes/main/main.tscn` — 添加 PauseMenu 和 GameOver CanvasLayer 节点
- `project.godot` — main_scene 改为 `res://scenes/main/boot.tscn`，添加 pause/ui_cancel 输入

---

## Task 1: SpinBlade 和 ChainLightning 武器场景

SpinBlade 和 ChainLightning 的 GDScript 已完整实现，但没有对应的 `.tscn` 场景文件。Godot 中 `Node.new()` 直接构造脚本类是合法的（P0 已在用），所以这两个武器通过代码实例化完全可行，无需 .tscn。

**但需要验证**：`level_up_panel.gd` 里已用 `SpinBlade.new()` 和 `ChainLightning.new()` 创建武器——这在 Godot 4 中对 `extends Node2D` 的类是合法的。我们只需确认 `exp_system.gd` 的武器池里三个 id 都能走到正确的 class。

**Files:**
- Verify: `scripts/ui/level_up_panel.gd:137-160` — `_apply_weapon_option` 已实现
- Verify: `scripts/systems/exp_system.gd:175-183` — 武器池含三个 id
- Modify: `scripts/systems/main_game.gd` — `_setup_player_visual` 改从 `GameManager.current_character_id` 读取角色

- [ ] **Step 1: 确认 .new() 实例化路径正确**

  打开 `scripts/ui/level_up_panel.gd`，确认第 137-170 行的 `_apply_weapon_option` 代码如下（已由 P0 实现）：

  ```gdscript
  match weapon_id:
      "bullet_ring":
          weapon = BulletRing.new()
          weapon.name = "BulletRing"
      "spin_blade":
          weapon = SpinBlade.new()
          weapon.name = "SpinBlade"
      "chain_lightning":
          weapon = ChainLightning.new()
          weapon.name = "ChainLightning"
  ```

  若已存在，此 Task 的武器实例化部分完成，继续 Step 2。

- [ ] **Step 2: 修改 main_game.gd 读取当前角色 id**

  修改 `scripts/systems/main_game.gd`：

  ```gdscript
  ## 设置玩家视觉
  func _setup_player_visual() -> void:
      if not player:
          return
      var char_id := GameManager.current_character_id
      if char_id.is_empty():
          char_id = "rin"
      var sprite := player.get_node_or_null("Sprite2D") as Sprite2D
      if sprite:
          sprite.texture = PixelSpriteGenerator.create_character_texture(char_id)
          sprite.texture_filter = CanvasItem.TEXTURE_FILTER_NEAREST
  ```

  同时修改 `start_game` 调用，保留 "rin" 作为回退（boot 场景 Task 2 会写入正确 id）：

  ```gdscript
  func _ready() -> void:
      _setup_player_visual()
      _add_starting_weapon()
      _setup_camera()
      _create_background()
      if GameManager.current_character_id.is_empty():
          GameManager.start_game("rin")
      else:
          GameManager.start_game(GameManager.current_character_id)
      print("[MainGame] 游戏场景初始化完成")
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add GoddessSurvivor/scripts/systems/main_game.gd
  git commit -m "feat: read character id from GameManager instead of hardcoded rin"
  ```

---

## Task 2: 角色选择界面

新建 boot 场景作为启动入口：先显示角色选择，点击后写入 `GameManager.current_character_id`，再切换到 `main.tscn`。

**Files:**
- Create: `scripts/ui/character_select.gd`
- Create: `scenes/main/boot.tscn`
- Modify: `project.godot` — `run/main_scene` 改为 boot.tscn

- [ ] **Step 1: 创建 character_select.gd**

  新建文件 `GoddessSurvivor/scripts/ui/character_select.gd`：

  ```gdscript
  ## 角色选择界面
  class_name CharacterSelect
  extends Control

  # 三个角色数据（与 character_data.gd 对应）
  const CHARACTERS := [
      {
          "id": "rin",
          "name": "凛 (Rin)",
          "desc": "均衡型 · 攻速爆发\n极限形态：攻击范围翻倍",
          "color": Color(1.0, 0.6, 0.7)
      },
      {
          "id": "lin",
          "name": "鳞 (Lin)",
          "desc": "近战型 · 高伤低速\n极限形态：护甲强化",
          "color": Color(0.4, 0.6, 1.0)
      },
      {
          "id": "rei",
          "name": "零 (Rei)",
          "desc": "法术型 · 冷却最短\n极限形态：技能范围+50%",
          "color": Color(0.7, 0.5, 1.0)
      },
  ]


  func _ready() -> void:
      _build_ui()


  func _build_ui() -> void:
      # 全屏深色背景
      var bg := ColorRect.new()
      bg.color = Color(0.08, 0.08, 0.12)
      bg.set_anchors_preset(Control.PRESET_FULL_RECT)
      add_child(bg)

      # 标题
      var title := Label.new()
      title.text = "选择角色"
      title.add_theme_font_size_override("font_size", 48)
      title.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
      title.set_anchors_preset(Control.PRESET_CENTER_TOP)
      title.position = Vector2(-200, 80)
      title.size = Vector2(400, 60)
      add_child(title)

      # 角色卡片容器（居中水平排列）
      var hbox := HBoxContainer.new()
      hbox.set_anchors_preset(Control.PRESET_CENTER)
      hbox.position = Vector2(-420, -120)
      hbox.size = Vector2(840, 280)
      hbox.add_theme_constant_override("separation", 40)
      add_child(hbox)

      for char_data in CHARACTERS:
          hbox.add_child(_create_char_card(char_data))


  func _create_char_card(char_data: Dictionary) -> PanelContainer:
      var panel := PanelContainer.new()
      panel.custom_minimum_size = Vector2(240, 280)

      var vbox := VBoxContainer.new()
      vbox.add_theme_constant_override("separation", 12)
      panel.add_child(vbox)

      # 角色像素图
      var texture_rect := TextureRect.new()
      texture_rect.texture = PixelSpriteGenerator.create_character_texture(char_data["id"])
      texture_rect.texture_filter = CanvasItem.TEXTURE_FILTER_NEAREST
      texture_rect.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
      texture_rect.custom_minimum_size = Vector2(120, 120)
      texture_rect.self_modulate = char_data["color"]
      vbox.add_child(texture_rect)

      # 角色名
      var name_label := Label.new()
      name_label.text = char_data["name"]
      name_label.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
      name_label.add_theme_font_size_override("font_size", 20)
      vbox.add_child(name_label)

      # 描述
      var desc_label := Label.new()
      desc_label.text = char_data["desc"]
      desc_label.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
      desc_label.autowrap_mode = TextServer.AUTOWRAP_WORD_SMART
      vbox.add_child(desc_label)

      # 选择按钮
      var btn := Button.new()
      btn.text = "选择"
      btn.add_theme_font_size_override("font_size", 18)
      var char_id: String = char_data["id"]
      btn.pressed.connect(func() -> void: _on_character_selected(char_id))
      vbox.add_child(btn)

      return panel


  func _on_character_selected(char_id: String) -> void:
      GameManager.current_character_id = char_id
      get_tree().change_scene_to_file("res://scenes/main/main.tscn")
  ```

- [ ] **Step 2: 创建 boot.tscn**

  新建文件 `GoddessSurvivor/scenes/main/boot.tscn`：

  ```
  [gd_scene load_steps=2 format=3]

  [ext_resource type="Script" path="res://scripts/ui/character_select.gd" id="1"]

  [node name="CharacterSelect" type="Control"]
  anchor_right = 1.0
  anchor_bottom = 1.0
  script = ExtResource("1")
  ```

- [ ] **Step 3: 修改 project.godot 启动场景**

  将 `project.godot` 中的：
  ```
  run/main_scene="res://scenes/main/main.tscn"
  ```
  改为：
  ```
  run/main_scene="res://scenes/main/boot.tscn"
  ```

  同时在 `[input]` 段末尾追加 pause 输入映射（Escape 键）：
  ```
  pause={
  "deadzone": 0.5,
  "events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":4194305,"physical_keycode":0,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
  ]
  }
  ```
  （keycode 4194305 = KEY_ESCAPE）

- [ ] **Step 4: Commit**

  ```bash
  git add GoddessSurvivor/scripts/ui/character_select.gd \
          GoddessSurvivor/scenes/main/boot.tscn \
          GoddessSurvivor/project.godot
  git commit -m "feat: add character select screen as boot scene"
  ```

---

## Task 3: 暂停菜单

按 Escape 键暂停/继续，界面上有"继续游戏"和"退出到桌面"按钮。

**Files:**
- Create: `scripts/ui/pause_menu.gd`
- Modify: `scenes/main/main.tscn` — 添加 PauseMenu CanvasLayer

- [ ] **Step 1: 创建 pause_menu.gd**

  新建 `GoddessSurvivor/scripts/ui/pause_menu.gd`：

  ```gdscript
  ## 暂停菜单
  class_name PauseMenu
  extends CanvasLayer

  var _panel: PanelContainer


  func _ready() -> void:
      layer = 20
      process_mode = Node.PROCESS_MODE_ALWAYS
      _build_ui()
      visible = false
      GameManager.state_changed.connect(_on_state_changed)


  func _unhandled_input(event: InputEvent) -> void:
      if event.is_action_just_pressed("pause"):
          if GameManager.current_state == GameManager.GameState.PLAYING:
              GameManager.pause_game()
          elif GameManager.current_state == GameManager.GameState.PAUSED:
              GameManager.resume_game()


  func _build_ui() -> void:
      var dim := ColorRect.new()
      dim.color = Color(0, 0, 0, 0.5)
      dim.set_anchors_preset(Control.PRESET_FULL_RECT)
      add_child(dim)

      _panel = PanelContainer.new()
      _panel.set_anchors_preset(Control.PRESET_CENTER)
      _panel.position = Vector2(-160, -120)
      _panel.size = Vector2(320, 240)
      add_child(_panel)

      var vbox := VBoxContainer.new()
      vbox.add_theme_constant_override("separation", 20)
      _panel.add_child(vbox)

      var title := Label.new()
      title.text = "暂停"
      title.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
      title.add_theme_font_size_override("font_size", 36)
      vbox.add_child(title)

      var resume_btn := Button.new()
      resume_btn.text = "继续游戏"
      resume_btn.add_theme_font_size_override("font_size", 22)
      resume_btn.pressed.connect(GameManager.resume_game)
      vbox.add_child(resume_btn)

      var quit_btn := Button.new()
      quit_btn.text = "退出到桌面"
      quit_btn.add_theme_font_size_override("font_size", 22)
      quit_btn.pressed.connect(func() -> void: get_tree().quit())
      vbox.add_child(quit_btn)


  func _on_state_changed(new_state: GameManager.GameState) -> void:
      visible = (new_state == GameManager.GameState.PAUSED)
  ```

- [ ] **Step 2: 将 PauseMenu 节点添加到 main.tscn**

  在 `GoddessSurvivor/scenes/main/main.tscn` 文件末尾（`[node name="WeaponManager"...]` 之后）追加：

  ```
  [ext_resource type="Script" path="res://scripts/ui/pause_menu.gd" id="8_pause"]

  [node name="PauseMenu" type="CanvasLayer" parent="."]
  script = ExtResource("8_pause")
  ```

  同时在文件头部 `load_steps=8` 改为 `load_steps=9`。

- [ ] **Step 3: Commit**

  ```bash
  git add GoddessSurvivor/scripts/ui/pause_menu.gd \
          GoddessSurvivor/scenes/main/main.tscn
  git commit -m "feat: add pause menu with Escape key toggle"
  ```

---

## Task 4: 游戏结束界面

游戏结束时显示本局统计（时间、击杀数、等级），提供"再来一局"（回到角色选择）和"退出"按钮。

**Files:**
- Create: `scripts/ui/game_over.gd`
- Modify: `scenes/main/main.tscn` — 添加 GameOver CanvasLayer

- [ ] **Step 1: 创建 game_over.gd**

  新建 `GoddessSurvivor/scripts/ui/game_over.gd`：

  ```gdscript
  ## 游戏结束界面
  class_name GameOver
  extends CanvasLayer


  func _ready() -> void:
      layer = 30
      process_mode = Node.PROCESS_MODE_ALWAYS
      visible = false
      GameManager.state_changed.connect(_on_state_changed)
      EventBus.game_over.connect(_on_game_over)


  func _on_game_over(kill_count: int, elapsed_time: float) -> void:
      _build_ui(kill_count, elapsed_time)
      visible = true


  func _on_state_changed(new_state: GameManager.GameState) -> void:
      if new_state != GameManager.GameState.GAME_OVER:
          visible = false


  func _build_ui(kill_count: int, elapsed_time: float) -> void:
      # 清除旧 UI（再来一局时会重建）
      for child in get_children():
          child.queue_free()

      var dim := ColorRect.new()
      dim.color = Color(0, 0, 0, 0.7)
      dim.set_anchors_preset(Control.PRESET_FULL_RECT)
      add_child(dim)

      var panel := PanelContainer.new()
      panel.set_anchors_preset(Control.PRESET_CENTER)
      panel.position = Vector2(-200, -180)
      panel.size = Vector2(400, 360)
      add_child(panel)

      var vbox := VBoxContainer.new()
      vbox.add_theme_constant_override("separation", 16)
      panel.add_child(vbox)

      var title := Label.new()
      title.text = "游戏结束"
      title.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
      title.add_theme_font_size_override("font_size", 40)
      vbox.add_child(title)

      # 统计
      var minutes := int(elapsed_time) / 60
      var seconds := int(elapsed_time) % 60
      var time_str := "%02d:%02d" % [minutes, seconds]

      for stat in [
          "存活时间: " + time_str,
          "击杀数: %d" % kill_count,
          "最终等级: Lv.%d" % GameManager.current_level,
      ]:
          var lbl := Label.new()
          lbl.text = stat
          lbl.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
          lbl.add_theme_font_size_override("font_size", 22)
          vbox.add_child(lbl)

      var retry_btn := Button.new()
      retry_btn.text = "再来一局"
      retry_btn.add_theme_font_size_override("font_size", 22)
      retry_btn.pressed.connect(func() -> void:
          get_tree().change_scene_to_file("res://scenes/main/boot.tscn")
      )
      vbox.add_child(retry_btn)

      var quit_btn := Button.new()
      quit_btn.text = "退出到桌面"
      quit_btn.add_theme_font_size_override("font_size", 22)
      quit_btn.pressed.connect(func() -> void: get_tree().quit())
      vbox.add_child(quit_btn)
  ```

- [ ] **Step 2: 将 GameOver 节点添加到 main.tscn**

  在 `main.tscn` 已有的 `[ext_resource ... id="8_pause"]` 之后追加：

  ```
  [ext_resource type="Script" path="res://scripts/ui/game_over.gd" id="9_gameover"]

  [node name="GameOver" type="CanvasLayer" parent="."]
  script = ExtResource("9_gameover")
  ```

  将文件头部 `load_steps=9` 改为 `load_steps=10`。

- [ ] **Step 3: Commit**

  ```bash
  git add GoddessSurvivor/scripts/ui/game_over.gd \
          GoddessSurvivor/scenes/main/main.tscn
  git commit -m "feat: add game over screen with stats and retry button"
  ```

---

## Task 5: EventBus 补全 game_over 信号签名

`game_over` 信号目前在 `event_bus.gd` 声明，但需要确认它带 `kill_count` 和 `elapsed_time` 两个参数，`game_manager.gd` emit 时也带这两个参数（已有）。

**Files:**
- Verify/Modify: `autoload/event_bus.gd`
- Verify: `autoload/game_manager.gd:70`

- [ ] **Step 1: 确认并修正 event_bus.gd 中的信号签名**

  打开 `GoddessSurvivor/autoload/event_bus.gd`，找到 `game_over` 信号，确认为：

  ```gdscript
  signal game_over(kill_count: int, elapsed_time: float)
  ```

  若当前无参数签名（`signal game_over`），改为带参数版本：

  ```gdscript
  signal game_over(kill_count: int, elapsed_time: float)
  ```

- [ ] **Step 2: Commit（仅当有改动时）**

  ```bash
  git add GoddessSurvivor/autoload/event_bus.gd
  git commit -m "fix: add parameters to game_over signal in EventBus"
  ```

---

## Task 6: 推送所有变更

- [ ] **Step 1: 确认所有修改已 commit**

  ```bash
  git status
  git log --oneline -6
  ```

  Expected: clean working tree，最新 6 条 commit 包含 Task 1-5 的内容。

- [ ] **Step 2: Push**

  ```bash
  git push origin main
  ```

---

## Self-Review

### Spec coverage
- ✅ SpinBlade / ChainLightning 可在游戏中获得（通过 .new() 已实现，Task 1 验证并补全 main_game.gd）
- ✅ 角色选择（Task 2）
- ✅ 暂停菜单（Task 3）
- ✅ 游戏结束界面（Task 4）
- ✅ EventBus signal 签名（Task 5）
- ❌ 服装阶段视觉变化（Sprite 仍是占位图）— 依赖真实美术资源，列入 P2，本计划不涉及
- ❌ Boss 战（15 分钟节点）— 列入 P2
- ❌ 协同觉醒 — 列入 P2
- ❌ 音效/BGM — 列入 P2

### Placeholder scan
无 TBD / TODO / "类似上方" 等占位词。

### Type consistency
- `GameManager.current_character_id: String` — 在 game_manager.gd:29 定义，Task 1/2 均使用此字段
- `GameManager.GameState.PAUSED/GAME_OVER` — 在 game_manager.gd:7 定义，Task 3/4 均使用
- `EventBus.game_over` 信号 — Task 4 connect，Task 5 修正签名，一致
- `PixelSpriteGenerator.create_character_texture(id)` — 在 pixel_sprite_generator.gd:25 定义，Task 2 使用，一致
